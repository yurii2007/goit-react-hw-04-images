import { ModalElem } from 'components/Modal/Modal.styled';
import { Radio } from 'react-loader-spinner';

export const Loader = () => {
  return (
    <ModalElem>
      <Radio />
    </ModalElem>
  );
};
